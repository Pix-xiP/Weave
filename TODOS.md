
- Add progress bar to sync and fetch commands
- Add 'notify' command to context to notify user via system.
- Plugin system, allow for more complex ideas to be loaded in via `Go` for speed while still allowing orchestration of intent via `Lua`
- If directory doesn't exist at dest, create it? prompt to create?
